name: dart_frog_api
description: SanskritAI Translator backend — REST API, translation orchestration, persistence.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  dart_frog: ^1.2.0
  shared_models:
    path: ../../packages/shared_models
  mongo_dart: ^0.10.3
  http: ^1.2.2
  uuid: ^4.5.1
  crypto: ^3.0.5
  dart_jsonwebtoken: ^2.14.2
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.8
  mocktail: ^1.0.4
  lints: ^4.0.0
